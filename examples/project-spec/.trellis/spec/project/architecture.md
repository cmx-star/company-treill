# 项目架构规范示例

> 请用目标项目的真实结构替换本页，不要保留示例性结论。

## 系统边界

记录项目负责的业务范围、外部依赖和明确不负责的内容。

## 模块职责

建议使用下表记录能够从代码验证的模块边界：

| 模块 | 路径 | 职责 | 允许依赖 | 禁止依赖 |
| --- | --- | --- | --- | --- |
| 待确认 | 待确认 | 待确认 | 待确认 | 待确认 |

## 数据流

描述一个典型请求如何经过入口、状态、领域逻辑、外部服务和持久化层。优先记录实际调用路径，不只列组件名称。

## 公共契约

列出修改前必须评估兼容性的内容：

- 对外 API、事件和消息格式。
- 数据库 Schema 和持久化文件。
- 公共组件、SDK 或包导出。
- 配置字段、环境变量和功能开关。

## 敏感区域

记录需要扩大验证或额外审批的目录和行为，例如认证、权限、支付、数据迁移、发布和跨版本兼容。
